export const header = () => {
    const header = document.createElement("header");
    header.id = "header"
    header.innerHTML = `
      <h1>El mundo a través de grandes escritorasa</h1>
        `;
  
    return header;
  };
